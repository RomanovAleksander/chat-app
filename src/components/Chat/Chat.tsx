import React from 'react';
import styles from './Chat.module.scss';

const Chat: React.FC = () => {
  return (
    <div className={styles.wrapper}>
      Chats
    </div>
  );
};

export default Chat;
